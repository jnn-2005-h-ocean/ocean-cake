


<div class="row", style="font-family: 'Hannotate SC',sans-serif;">
  <div class="col-xs-7">
    <div class="mypage-title">
      <mark style="font-size: 20px;">マイページ</mark>
    </div>
    <div>
      <h2 class="main-title">登録情報
        <%=  link_to "編集する", edit_customers_customer_path(current_customer.id), class:"btn btn-primary" %>
        <%=  link_to "パスワードを変更する", edit_customer_registration_path, class:"btn btn-primary",style:"margin-left:10px;" %>
      </h2>
    </div>
    <table class="table table-bordered">
      <tr>
        <th>氏名</th>
        <td>
          <%= "#{current_customer.family_name}　#{current_customer.first_name}" %>
        </td>
      </tr>

      <tr>
        <th>カナ</th>
        <td>
          <%= "#{current_customer.family_name_kana}　#{current_customer.first_name_kana}" %>
        </td>
      </tr>

      <tr>
        <th>郵便番号</th>
        <td>
          <%= current_customer.postal_code %>
        </td>
      </tr>

      <tr>
        <th>住所</th>
        <td>
          <%= current_customer.address %>
        </td>
      </tr>

      <tr>
        <th>電話番号</th>
        <td>
          <%= current_customer.telephone_number %>
        </td>
      </tr>

      <tr>
        <th>メールアドレス</th>
        <td>
          <%= current_customer.email %>
        </td>
      </tr>
    </table>
    <div>
      <h2 class="main-title" style="margin-right:26px;">配送先
        <%=  link_to "一覧を見る", customers_customer_addresses_path(current_customer.id), class:"btn btn-primary"%>
      </h2>
    </div>

    <div>
      <h2 class="main-title">注文履歴
        <%=  link_to "一覧を見る", customers_customer_order_items_path(current_customer.id), class:"btn btn-primary" %>
      </h2>
    </div>
  </div>
</div>